# UI Components

shadcn/ui base components. These are the building blocks for all features.

## Adding Components

```bash
pnpm dlx shadcn@latest add button
```

Components are installed individually and committed to the repo (not a package dependency).

## What's Here

See [shadcn/ui documentation](https://ui.shadcn.com/docs/components) for all available components.
