import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function LoginPage() {
  return (
    <Card className="w-full max-w-sm">
      <CardHeader>
        <CardTitle>Login (E10)</CardTitle>
        <CardDescription>Auth form placeholder — full implementation in E10</CardDescription>
      </CardHeader>
    </Card>
  );
}
